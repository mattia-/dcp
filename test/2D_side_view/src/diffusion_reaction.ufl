# UFL file to define a diffusion reaction problem 
#
#
# define mesh dimension
cell = interval

# define function space
V= FiniteElement ("Lagrange", cell, 1)

# define coefficients
f = Coefficient (V)

# define functions
u = TrialFunction (V)
v = TestFunction (V)

# define linear form
L = inner (-f, v) * dx

# define bilinear form
a = inner (grad (u), grad (v)) * dx + inner (u, v) * dx
