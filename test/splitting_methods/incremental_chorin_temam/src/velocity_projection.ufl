# third step of the Chorin-Temam splitting method

# domain type
cell = triangle

# function space
V = VectorElement ("Lagrange", cell, 2)
Q = FiniteElement ("Lagrange", cell, 1)

# coefficients
dt = Constant (cell)
u_star = Coefficient (V)
dp = Coefficient (Q)

# test and trial function
u = TrialFunction (V)
v = TestFunction (V)

# projection equation:
a = inner (u, v) * dx

# rhs
L = inner (u_star, v) * dx - dt * inner (grad (dp), v) * dx
